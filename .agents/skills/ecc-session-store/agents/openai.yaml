interface:
  display_name: "Ecc Session Store"
  short_description: "Repo-specific patterns and workflows for ecc-session-store"
  default_prompt: "Use the ecc-session-store repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true